/**
 * Twitter Card Image Generator for SLEEK Dental
 *
 * Re-exports the OpenGraph image for Twitter cards. This ensures consistent
 * branding across all social platforms while using the same premium design.
 *
 * Twitter uses the summary_large_image card type which displays the image
 * prominently in tweets containing links to the site.
 */
export { default, runtime, alt, size, contentType } from './opengraph-image'
